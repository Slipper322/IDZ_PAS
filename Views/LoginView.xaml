<Window x:Class="ComputerClubDiplom.Views.LoginView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ComputerClubDiplom.Views"
        mc:Ignorable="d"
		Title="Авторизация" Height="300" Width="400" WindowStartupLocation="CenterScreen">
	<Grid>
		<StackPanel HorizontalAlignment="Center" VerticalAlignment="Center" Width="250">
			<Label Content="Логин:" Margin="0,0,0,5"/>
			<TextBox Text="{Binding Email, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,10"/>
			<Label Content="Пароль:" Margin="0,0,0,5"/>
			<PasswordBox x:Name="PasswordBox" Margin="0,0,0,20"/>
			<StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
				<Button Content="Войти" Command="{Binding LoginCommand}" Width="100" Margin="0,0,10,0" Click="Button_Click"/>
			</StackPanel>
		</StackPanel>
	</Grid>
</Window>
